---
description: Score a resume, LinkedIn profile or README and name what is wrong, without rewriting it
argument-hint: "[paste the document, or name a file, and say which surface it is]"
---

Use the `profile-optimiser` skill in audit mode. Do not rewrite the text.

Identify the surface first (resume, LinkedIn, GitHub profile README, project README) and read the matching section of the surface guide before judging anything against convention.

Return the audit report format: verdict, lines that fail the substitution test with who else could write them, lines missing evidence with what would fix them and where they would find it, AI tells named by pattern and number, structure and convention problems, what is already working, and one biggest lift. Quote the actual lines. Drop any empty section rather than writing "none found" under it.

Do not rewrite, and do not state or imply a verdict about whether a person or a model wrote the text. Offer the rewrite as the next step.

Document:

$ARGUMENTS
